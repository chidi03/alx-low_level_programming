#include "main.h"
/**
 * cap_string - capitalizes all the words of a string.
 * @str: the string to be capitalized.
 * Return: a pointer to the changed string.
 */
char *cap_string(char *str)
{
	int index = 0;

	while (str[index])
	{
		while (!(str[inedx] >= 'a' && str[index] <= 'z'))
			index++

		if (str[index - 1] == ' ' || str [index - 1] == '\t'
	}
}
