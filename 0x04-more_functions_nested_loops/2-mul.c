#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first integer to be multiplied.
 * @b: second integer to be multiplied.
 * Return: result.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
