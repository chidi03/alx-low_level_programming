#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * File - 3-function-like-macro.h
 * auth: chidi03
 * desc: header files that defines a function-like macro ABS(x) that computes
 *		the absolute value of a number x.
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
